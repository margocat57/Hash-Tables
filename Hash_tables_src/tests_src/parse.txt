tr -c '[:alpha:]' ' ' < input.txt | tr ' ' '\n' | grep -v '^$' | sort -u > output.txt 

sort -u words_alpha.txt -o words_alpha.txt

tr -s '[:space:]' '\n' < words_alpha.txt | awk '{if(length>max){max=length;word=$0}} END{print word}'

cat words_alpha.txt >> words.txt 
shuf words.txt -o words.txt 

popen("wc -l tests_src/input.txt", "r"); - работаем с файлов (fscanf)


cat /sys/devices/system/cpu/cpu3/topology/core_id - узнаем какой core_id у 3 cpu(3 логическом ядре), чтобы уже считывать датчики
cat /sys/devices/system/cpu/cpu3/cpufreq/scaling_cur_freq   -  узнаем какая частота
cat /sys/class/hwmon/hwmon1/temp22_label  - Core 20
cat /sys/class/hwmon/hwmon1/temp22_input - температура